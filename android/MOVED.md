# Android development moved

Active Android work continues in the sibling project:

`D:\CocosLoc\AFKLLM-Android` (separate git repo)

This `android/` tree in **AFKLLM** (`android` branch) is a snapshot / archive so the PC Electron app on `master` stays untouched.
